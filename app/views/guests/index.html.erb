<h1>All The Guests</h1>
<%= button_to "List of Episodes", episodes_path, :method => :get %>

<ul>
  <% @guests.each do |guest| %>
  <%=  link_to "#{guest.name}", guest_path(guest) %><br />
  <% end %>
</ul>
